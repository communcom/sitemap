const env = process.env;

module.exports = {
    GLS_PRISM_MONGO_CONNECT: env.GLS_PRISM_MONGO_CONNECT,
    GLS_GENERATE_EVERY: Number(env.GLS_GENERATE_EVERY) || 30 * 60 * 1000,
    GLS_FILL_EVERY: Number(env.GLS_GENERATE_EVERY) || 30000,
    GLS_DESTINATION_FOLDER: env.GLS_DESTINATION_FOLDER || './sitemap',
    GLS_HOSTNAME: env.GLS_HOSTNAME || 'https://commun.com',
    GLS_SITEMAP_SIZE: Number(env.GLS_SITEMAP_SIZE) || 40000,
    GLS_POSTS_REQUEST_LIMIT: Number(env.GLS_POSTS_REQUEST_LIMIT) || 1000,
    GLS_POSTS_REQUEST_INTERVAL: Number(env.GLS_POSTS_REQUEST_INTERVAL) || 10000,
    GLS_LATE_DAYS_COUNT: Number(env.GLS_LATE_DAYS_COUNT) || 7,
};
